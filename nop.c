#include "monty.h"

/**
 * nop - a function which doesn't do anything
 * @stack: a double pointer to the stack linked list
 * @line_number: the line number in the file
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
